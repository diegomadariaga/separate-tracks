{
  "name": "@separate-tracks/api",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.app.json",
    "build:watch": "tsc -p tsconfig.app.json -w",
    "start": "node dist/out-tsc/src/main.js",
    "dev": "concurrently \"pnpm build:watch\" \"nodemon dist/out-tsc/src/main.js\"",
    "typecheck": "tsc -p tsconfig.app.json --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.0",
  "@separate-tracks/shared-types": "workspace:*"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "nodemon": "^3.1.0"
  }
}
