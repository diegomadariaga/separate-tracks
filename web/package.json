{
  "name": "@separate-tracks/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "typecheck": "tsc -p tsconfig.app.json --noEmit"
  },
  "dependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "axios": "^1.6.0",
  "@separate-tracks/shared-types": "workspace:*"
  },
  "devDependencies": {
    "vite-tsconfig-paths": "^5.1.0"
  }
}
